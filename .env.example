# =============================================
#  COPY THIS FILE TO .env AND FILL IN YOUR VALUES
#  .env is listed in .gitignore — it will NEVER
#  be uploaded to GitHub
# =============================================

DB_PASSWORD=your_postgresql_password
DB_USER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=job_postings
